# RUNNOW 컨텍스트 노트 (의사결정 기록)

## 2026-09-04 — AI 모션 인식 안정화 & 난이도 분화

### 왜 카운트가 부정확했는가 (코드 근거)
1. **준비 페이즈가 아예 없음.** `startCamera()`가 `predictWebcamLoop()`을 띄우는 순간부터
   `processExerciseLogic()`이 rep을 셉니다. 사용자가 자리를 잡는 동안 이미 카운트가 돕니다.
2. **윗몸일으키기 즉발 카운트 버그.** 생성자에서 `motionState = "up"`으로 시작하는데,
   situp 로직은 `angle >= 130 && motionState === "up"` 이면 rep을 등록합니다.
   서 있을 때 어깨-골반-무릎 각도는 약 170도라 **카메라를 켜자마자 1회가 올라갑니다.**
3. **visibility 검증 없음.** MediaPipe는 프레임 밖 관절도 추정 좌표를 반환합니다.
   상반신만 잡힌 상태에서도 무릎 각도가 계산되어 유령 rep이 발생합니다.
4. **좌우 평균의 오염.** `(leftAngle + rightAngle) / 2`는 측면 촬영처럼 한쪽이 가려진
   상황에서 가려진 쪽의 추정치가 평균을 끌어당깁니다.
5. **스무딩·디바운스 부재.** 원시 각도가 임계값 근처에서 떨릴 때 중복 카운트가 납니다.
   `lastRepTimestamp` 필드는 선언만 되고 한 번도 사용되지 않았습니다.

### 채택한 구조
- **페이즈 머신**: `idle → calibrating → countdown → counting`.
  `counting` 이전에는 `registerRep()`이 호출될 수 없는 구조로 만들었습니다.
  타이머와 칼로리 누적도 `counting` 진입 시점부터 시작합니다.
- **준비 게이트 통과 조건**: (a) 해당 운동의 필수 관절이 모두 visibility 기준 이상,
  (b) 시작 자세(신전 구간)에 들어와 있음, (c) 이 상태가 난이도별 `readyHoldMs` 동안 연속 유지.
  세 조건이 무너지면 누적 시간이 0으로 리셋되어 "우연히 통과"하지 않습니다.
- **rep 판정을 제네릭 FSM으로 통합**: 스쿼트/푸시업/윗몸일으키기/런지는 모두
  "신전 → 수축 → 신전" 사이클입니다. 운동마다 복붙된 if/else를 `runRepCycle()` 하나로 합쳤습니다.
  초기 페이즈는 `null`이며, **신전 구간을 한 번 관측해야만** 사이클이 시작됩니다.
  이것이 위 2번 버그의 근본 수정입니다.

### 난이도 설계 — 왜 배열이 아니라 오프셋인가
운동 6종 × 난이도 3단계의 임계값을 전부 나열하면 18세트를 손으로 관리해야 합니다.
대신 운동별 **기준 ROM**(`rom.contracted` / `rom.extended`)을 두고, 난이도는
`contractedOffset` / `extendedOffset`으로 표현했습니다.
- 초보자: 수축 기준 +22도(덜 앉아도 인정), 신전 기준 -14도(덜 펴도 인정)
- 중급자: 오프셋 0 (기존 값과 동일 = 기존 사용자 경험 유지)
- 단련자: 수축 -13도, 신전 +8도 + 수축 하단 유지시간 요구 + 폼 엄격 판정

난이도는 임계값만이 아니라 **visibility 기준, rep 최소 간격, 준비 유지시간, 폼 엄격도**까지
함께 움직입니다. 초보자는 관대하게 통과시키고, 단련자는 반동(치팅) 반복을 걸러냅니다.

### 논의가 필요한 판단 (되돌리기 쉬움)
`getWorkoutSummary()`에 난이도별 보상 배율(0.9 / 1.0 / 1.15)을 넣었습니다.
난이도가 난도만 올리고 보상이 같으면 단련자 모드를 고를 이유가 없기 때문입니다.
다만 이는 **인게임 경제에 영향을 주는 변경**이므로, 원치 않으면
`DIFFICULTY_LEVELS[*].rewardMultiplier`를 전부 1.0으로 두면 즉시 무력화됩니다.

### 손대지 않은 것 (의도적)
- `workout.html` (596줄) — 어떤 파일에서도 참조되지 않는 고아 파일입니다.
  index.html 안의 모션 스튜디오가 실제 사용 경로입니다. 삭제는 하지 않고 보고만 합니다.
- `firebaseSandbox.js`, PayPal, 퀘스트, GPS 로직 — 이번 요청 범위 밖이라 유지했습니다.

## 2026-09-04 (2) — 여러 사람이 있을 때 남의 동작이 카운트되던 문제

### 원인
MediaPipe는 **프레임마다 사람을 새로 고릅니다.** 동일인을 이어서 추적한다는
보장이 없어서, 옆 사람이 더 크거나 선명하게 잡히면 그쪽으로 갈아탑니다.
기존 코드는 `poses[0]`을 그대로 썼기 때문에 "아까 그 사람인가"를 확인하는
장치가 전혀 없었습니다. Classic Pose 엔진은 아예 1명만 반환합니다.

### 채택한 구조 — 운동자 잠금(Subject Lock)
얼굴 인식(임베딩)은 MediaPipe Pose에 없고 온디바이스 비용도 큽니다.
대신 33개 관절 중 어깨(11,12)·골반(23,24)만으로 **신체 서명**을 만들었습니다.
- `center` — 몸통 중심 좌표 (어디에 있는가)
- `scale`  — 어깨너비 + 몸통길이 (카메라와의 거리)
- `ratio`  — 어깨너비 / 몸통길이 (체형)

위치 차이는 몸 크기로 나눠 정규화하므로 카메라 거리와 무관하게 동작합니다.
준비 자세를 통과한 순간 이 서명을 잠그고, 이후 프레임마다 EMA(0.3)로
따라가며 갱신합니다. 갱신하지 않으면 몇 초 만에 본인을 놓칩니다.

- Tasks Vision(`numPoses: 3`) — 여러 후보 중 **서명이 가장 가까운 사람**을 선택
- Classic Pose(1명만 반환) — 후보 선택은 못 하지만, 반환된 사람이 잠긴
  운동자인지 **검증은 동일하게** 수행. 아니면 프레임을 버립니다.

### 임계값을 어떻게 정했나
가장 위험한 실패는 "너무 빡빡해서 운동 중인 본인을 놓치는 것"입니다.
그래서 `SUBJECT_MATCH_TOLERANCE = 1.6`으로 관대하게 두고, 대신
**여러 명 중 최적 매칭을 고르는 것**을 1차 방어선으로 삼았습니다.
tests의 13번 케이스(스쿼트/윗몸일으키기 3회 전 구간 + 한 걸음 이동)가
오탐 회귀를 막습니다. 이 테스트가 깨지면 실사용에서 "운동 중인데 자꾸
준비로 돌아가는" 문제가 납니다.

### 자세를 벗어났을 때
- 1.2초 이상 운동자를 놓치면 `calibrating`으로 되돌아갑니다.
  숫자가 올라가지 않고 "다시 시작 자세를 잡아 주세요"가 표시됩니다.
- **이때 기존 횟수와 기록은 보존됩니다.** 세트 도중에 잠깐 벗어난 것이지
  운동을 새로 시작하는 게 아니기 때문입니다. 시간 측정만 멈춥니다.
- 10초 이상 완전히 사라지면 잠금을 풀어 다른 사람이 새로 시작할 수 있게 합니다.

### 알려진 한계 (정직하게 기록)
Classic Pose 엔진에서는 후보를 고를 수 없어, 본인이 프레임을 벗어난 사이
바로 옆(체형·거리까지 비슷한)에 있는 사람이 잡히면 통과할 여지가 있습니다.
Tasks Vision 경로에서는 최적 매칭으로 걸러집니다. 실사용에서 문제가 되면
엔진 우선순위를 Tasks Vision으로 바꾸는 것을 검토해야 합니다.

## 2026-09-04 (3) — 준비 게이트 탈출구 & 엔진 가시화

### 왜 탈출구가 필요했나
준비 게이트는 필수 관절이 모두 보여야 통과합니다. 좁은 방·어두운 조명·
폰 각도 때문에 전신이 안 잡히면 **영영 카운트를 시작할 수 없는** 구조였습니다.
이건 제가 준비 게이트를 넣으면서 만든 위험입니다.

주의할 점은 "그냥 시작" 버튼만으로는 해결되지 않는다는 것입니다.
`measurePose()`가 visibility 미달이면 null을 반환하므로, 카운팅 페이즈에
들어가도 횟수가 오르지 않습니다. 그래서 **인식 기준 자체를 낮추는**
완화 모드로 설계했습니다.

### 완화 모드의 경계 — 무엇을 풀고 무엇을 안 푸는가
- 푸는 것: `minVisibility`만 0.25로 하향 (관절이 흐릿해도 판정 시도)
- **안 푸는 것**: 가동범위(contracted/extended), 하단 정지 시간, rep 디바운스
  → "덜 굽혀도 인정"되는 치팅 통로가 되지 않도록 했습니다. 테스트 15번이 이를 고정합니다.
- 12초 이상 막혔을 때만 버튼이 노출되고, 전환 시 화면에 정확도 저하를 명시합니다.
- 새 세션(resetExerciseStats)에서 자동 해제되어 다음 운동에 끌려가지 않습니다.

### 엔진 표시를 넣은 이유
Classic Pose는 1명만 반환해 다중인원 선별이 불가능하고, Tasks Vision은 가능합니다.
어느 엔진이 도는지에 따라 인식 문제의 성격이 완전히 달라지는데 지금까지는
콘솔에만 찍혔습니다. HUD 좌하단에 엔진/감지 인원수/완화 여부를 표시해
사용자 신고만으로 원인을 좁힐 수 있게 했습니다.

### 함께 고친 결함
`toggleCamera`가 운동자 잠금을 지우지 않아, 전/후면 전환 시 낡은 신체 서명이
계속 매칭에 실패해 10초 해제 타이머가 돌 때까지 "운동하시던 분을 찾는 중"에
갇혔습니다. 전환 시 즉시 잠금을 해제합니다.

## 2026-09-07 — 세트 종료 후 AI 폼 리포트

### 배경
"이 기술로 PT 선생을 얻는 것이 중요하다"는 방향. 기존에는 틀렸을 때 지적만 하고,
세트가 끝나면 횟수·칼로리만 보여줬습니다.

### 이미 계산 중인데 버려지던 데이터를 회수
- `cycleDeepestAngle` — 회차별 최저 각도. rep 완료 시 폐기되던 값
- `contractedEnteredAt` ↔ 하강 시작 시각 — 내리기/올리기 템포
- `bestSideAngle`의 좌우 각도 차이 — 그동안 평균만 쓰고 버리던 값

`bestSideAngle`이 `sideDelta`(왼쪽−오른쪽)를 함께 반환하도록 하고,
회차마다 `repLog`에 { deepest, descentMs, ascentMs, sideDelta }를 쌓습니다.
**카운트로 인정된 회차만** 기록합니다(디바운스로 거부된 반동은 통계를 오염시킵니다).

### 하강 시작 시점을 어떻게 잡았나
`cycleStartedAt`(신전 진입 시각)을 쓰면 상단에서 쉬는 시간이 하강 시간에 섞입니다.
그래서 **신전 구간을 벗어나는 첫 프레임**을 `descentStartedAt`으로 잡았습니다.
템포가 "실제로 움직인 시간"을 반영합니다.

### 표시 기준
- 3회 미만이면 `buildFormReport()`가 null → UI에서 섹션을 통째로 숨깁니다.
  표본이 적으면 평균·최저 회차가 의미 없는 조언이 됩니다.
- 좌우 불균형은 **양쪽 관절이 모두 선명한 회차가 3회 이상**이고 평균 편차가
  6도 이상일 때만 보고합니다. 측면 촬영에서는 sideDelta가 null이라 자동 제외됩니다.
- 난이도 상향 제안은 **전 회차가 기준보다 12도 이상 깊었을 때만**.
  사용자 선택(제안만 표시)에 따라 원터치 변경 버튼은 두지 않았습니다.

### 노출 위치
사용자 결정에 따라 **기존 완료 모달 안**에 붙였습니다. 새 화면을 만들면
보상 수령 흐름이 끊기고, 라우팅을 새로 만들어야 합니다.

## 2026-09-07 (2) — GPS 거리 계산 검증 및 명세 위반 수정

### 왜 GPS부터 봤나
거리가 곧 보상(XP·볼트코인·다마고치 성장)입니다. 계산이 틀리면 경제가 무너지는데
검증이 전혀 없었습니다. 모션 쪽에서 나온 것과 같은 종류의 버그를 예상했고, 실제로 나왔습니다.

### 발견 1 — 정확도 필터가 구현되어 있지 않았다
기획서: "정확도 반경(Accuracy) 25m 초과 데이터 폐기"
실제 코드: 정확도를 **문구로 표시만** 하고("GPS 약함") 그 점의 거리를 그대로 누적.
오차 반경 ±80m짜리 측정치도 거리로 잡혔습니다. 실내·도심에서 가만히 있어도
거리가 늘어나는 원인입니다. `MAX_ACCURACY_M = 25`로 폐기하도록 수정.

거리가 왜 안 늘어나는지 알 수 없으면 사용자가 고장으로 오해하므로,
상태 문구를 "GPS 정확도 낮음 (±Xm) · 거리 미집계"로 명시했습니다.

### 발견 2 — 안티치트 임계값이 문서의 3배
기획서: "시속 30km/h 초과 이동 구간(차량 탑승 등) 자동 감지 및 거리 배제"
실제 코드: `speedCheck <= 25` — 초당 25m는 **시속 90km**입니다.
코드 주석에도 "시속 90km"라고 적혀 있어 의도적 값이 아니라 단위 혼동으로 보입니다.
도심 주행(시속 60km)이 전부 러닝 거리로 인정되고 있었습니다.
`MAX_SPEED_MPS = 30 / 3.6`으로 수정.

### 발견 3 — 거부된 구간이 정차 순간 한꺼번에 인정되는 누수
속도 초과로 거부해도 `lastValidPos`를 그대로 두면, 차에서 내려 시간이 흐른 뒤
dt가 커지면서 speedCheck가 임계값 아래로 떨어져 **누적 변위 전체가 통과**합니다.
테스트에서 확인: 시속 60km로 30초 이동(약 500m) 후 60초 정차하면
speedCheck가 8.4로 떨어져 경계선에 걸립니다. 조금만 더 기다리면 전부 인정됩니다.

처음에는 15초 후 재설정(REANCHOR_AFTER_MS)으로 막으려 했으나, 그래도 최대
15초치 거리가 새고 튜닝 상수가 하나 늘어납니다. **속도 초과 거부 시 거리는 더하지 않고
기준점만 즉시 옮기는** 방식으로 단순화했습니다. 상수도 필요 없고 누수도 0입니다.

부작용: GPS 스파이크 1회당 직후 정상 측정 1건의 거리(수 m)를 잃습니다. 무시할 수준입니다.

### 검증하지 않은 것
- 트레드밀 모드(`tickTreadmill`, DeviceMotion 기반 걸음 감지) — 실기기 센서가 필요합니다
- `startRun` 내부의 geolocation 연동 경로 — 브라우저 API라 단위 테스트 대상이 아닙니다
테스트는 `handleGeoSuccess`와 `getStats`의 순수 계산부만 다룹니다.

## 2026-09-07 (3) — 결제·구독 서버 검증

### 왜 필요했나
- 구독 상태가 `localStorage`에만 있었습니다. 콘솔 한 줄로 PRO가 됩니다.
- `paypalBridge.js`는 `setTimeout` 후 가짜 주문번호를 만드는 **UI 시뮬레이션**이었습니다.
  기획서의 "PayPal REST API v2 + 결제 검증/캡처"는 구현된 적이 없습니다.
- `firestore.rules`에 subscriptions 규칙이 아예 없었습니다.

### 전제 확인 — 인증이 진짜인가
서버 검증은 신뢰할 수 있는 uid가 있어야 성립합니다. `firebaseClient.js`가
`getAuth` / `signInWithPopup` / `createUserWithEmailAndPassword`를 쓰는
**실제 Firebase Auth**임을 먼저 확인한 뒤 진행했습니다. 가짜 인증 위에 얹었으면 무의미했습니다.

### 구조
클라이언트는 `planId`와 `orderId`만 보냅니다. **금액은 절대 보내지 않습니다.**
`functions/plans.js`가 서버 정본이며, 클라이언트가 보낸 가격을 쓰면 1센트 결제로
PRO를 살 수 있습니다.

캡처 단계에서 재검증하는 것들.
- `status === "COMPLETED"`
- 결제 금액·통화가 상품 정본과 일치
- 주문 소유자 uid 대조 (타인 결제로 내 계정 업그레이드 차단)
- `payment_orders`에 CAPTURED 기록 → 같은 주문 재사용 차단 (1회 결제 무한 연장 방지)

### firestore.rules가 실질적 방어선
`subscriptions/{userId}`는 **본인 읽기만 허용, 쓰기 전면 차단**입니다.
Admin SDK는 규칙을 우회하므로 Cloud Functions만 쓸 수 있습니다.
클라이언트가 쓸 수 있으면 PRO 무료 발급과 같습니다.

### localStorage의 위상 변경
캐시로 강등했습니다. `applyServerState()`가 서버 응답으로 항상 덮어쓰고,
`verifiedBy: "server"` 표식을 남깁니다. 앱 시작과 로그인 직후
`syncSubscriptionFromServer()`가 호출됩니다.

함수 미배포·오프라인이면 `fetchMySubscription()`이 조용히 null을 반환해
로컬 캐시로 동작합니다. 서버가 없다고 앱이 멈추면 안 되기 때문입니다.

### 대표님이 직접 해야 하는 것 (제가 할 수 없음)
1. **Firebase Blaze 플랜 전환** — Spark에서는 Cloud Functions가 외부 네트워크를
   호출할 수 없어 PayPal API를 부를 수 없습니다. 유료 전환 결정은 대표님 몫입니다.
2. **PayPal Client ID / Secret 발급 및 등록** — 시크릿은 제가 보거나 대신 입력하지 않습니다.
   `firebase functions:secrets:set` 으로 직접 넣으셔야 합니다.
자세한 절차는 `functions/README.md`에 정리했습니다.

### 정직하게 남겨둔 한계
AI 모션 인식은 기기에서 돌기 때문에, 클라이언트 코드를 고치면 화면 잠금 자체는
우회할 수 있습니다. 온디바이스 구조의 본질적 한계입니다.
서버 검증이 보장하는 것은 **결제 무결성**(실제 결제 여부, 금액 정확성, 환불 시 회수)이며,
콘텐츠 보호가 아닙니다. 콘텐츠까지 막으려면 서버 렌더링이 필요하고 이는
온디바이스 $0 서버비 전략과 상충합니다.

### 아직 교체하지 않은 것
`paypalBridge.js`의 결제 모달은 그대로 두었습니다. 지금은 사용자 확인 UI 역할만 하고,
실제 주문 생성·캡처는 `verifySubscriptionOnServer()`가 Cloud Functions로 처리합니다.
PayPal JS SDK 버튼으로 교체하려면 별도 작업이 필요합니다.

## 2026-09-07 (4) — 다마고치·퀘스트 보상 로직 검증

### 발견 1 (심각) — 버튼 연타로 XP 무한 획득
`feed()` / `play()` / `rest()` / `rescueVolt()`에 쿨다운도 비용도 없었습니다.
`lastFed` 필드는 저장만 되고 **한 번도 검사되지 않았습니다** — 쿨다운을 넣으려다 만 흔적입니다.

테스트로 실측: **버튼 600회 클릭에 레벨 1 → 9.**
`rest()`가 기력을 채우고 `play()`가 소모하므로 두 버튼을 번갈아 누르면 무한 루프이며,
`feed()`가 배고픔까지 채워 줍니다. "달리는 만큼 진화한다"는 앱의 전제가 무너집니다.

수정: 엔진에 액션별 쿨다운 도입(먹이 4h / 놀기 2h / 휴식 3h / 힐링 12h).
UI가 아니라 **엔진**에 넣은 이유는, 화면을 우회해도 막혀야 하기 때문입니다.
`toJSON()`에 `lastActionAt`을 포함시켜 새로고침으로 초기화되지 않게 했습니다.
쿨다운 값은 밸런스 판단이므로 `ACTION_COOLDOWNS` 상수 한 곳에서 조정 가능합니다.

`play()`는 기력 검사를 쿨다운보다 먼저 둡니다. 기력이 부족해 실패했을 때
쿨다운까지 소모되면 사용자가 손해를 봅니다.

### 발견 2 — 화면에 표시되는 스탯 증가량이 실제와 달랐다
`addKmAndWorkout()`이 상한 100에 걸려 실제로는 +2만 올랐는데
반환값에는 요청치(+85)가 그대로 담겨 화면에 "+85 성장"으로 표시됐습니다.
실제 반영분만 보고하도록 수정했습니다.

### 발견 3 — 퀘스트 판정이 두 곳에 복붙되어 있고, 수령 시 재검증이 없었다
`quests.js`는 순수 데이터였고 판정 로직은 app.js의 렌더 함수 안에 있었습니다.
같은 조건이 `normalDoneCount` 필터와 forEach에 **두 번 복붙**돼 있어
한쪽만 고치면 화면과 실제가 어긋납니다.

더 중요한 것은 `claimDailyReward()`가 **달성 여부를 전혀 확인하지 않았다**는 점입니다.
"이미 수령했는가"만 보고 보상을 지급했습니다. 달성 판정은 렌더링이 버튼을
그릴지 말지 결정할 때만 쓰였습니다.

수정: 판정을 `quests.js`로 옮겨 단일 출처로 만들고(`isDailyQuestAchieved`,
`isWeeklyQuestAchieved`, `countDailyDone`), 렌더와 수령 양쪽이 같은 함수를 쓰게 했습니다.
수령 시점에 다시 검증하므로 렌더 타이밍 문제로 버튼이 잘못 노출돼도 보상이 나가지 않습니다.

부수 효과로 판정 로직이 순수 함수가 되어 테스트 가능해졌습니다(빈 통계에서
undefined 비교로 달성 처리되는 사고를 회귀 테스트로 막습니다).

### 한계 — 클라이언트 경제의 본질
`firebaseSandbox`가 localStorage이므로, 마음먹은 사용자는 코인·XP를 직접 고칠 수 있습니다.
이번 수정이 막는 것은 **정상 UI 조작만으로 발생하는 무상 보상**과 **로직 불일치 버그**입니다.
경제를 진짜로 지키려면 보상 지급도 Cloud Functions로 옮겨야 하며, 이는 결제 검증과
같은 구조입니다(2026-09-07 (3) 참조).

## 2026-09-08 — 21일 챌린지 스트릭 검증

### 발견 1 — 끊긴 스트릭이 그대로 남아 보상을 준다
`this.streak`은 `tryCompleteFromWorkout()` 안에서만 갱신됩니다. 즉 **마지막으로
완료한 날 기준의 값**이고, 중단해도 옛 숫자가 그대로 남습니다.

이 값이 두 곳에 쓰이고 있었습니다.
- 화면: "🔥 3일 연속" → 2주를 쉬어도 계속 3일 연속으로 표시
- 주간 퀘스트 wq_02(`streak >= 3`) → 달리지 않아도 매주 보상 수령 가능

수정: `getCurrentStreak()`을 추가했습니다. 마지막 완료가 오늘(gap 0) 또는
어제(gap 1)면 살아 있고, 그보다 오래되면 0입니다.

**저장된 `streak`은 건드리지 않았습니다.** 읽을 때 계산하는 파생값으로 두는 편이
안전합니다. 완료 시점의 `gap > 1 ? 1 : streak + 1` 로직이 이미 리셋을 처리하므로,
읽기 경로에서 상태를 변경할 이유가 없습니다.

### 발견 2 — 일일 퀘스트가 자정이 아니라 오전 9시에 리셋된다
`app.js`가 날짜 키를 `new Date().toISOString().slice(0, 10)`으로 만들고 있었습니다.
`toISOString()`은 UTC 기준이라 한국(UTC+9)에서는 하루가 9시간 밀립니다.

실측:
```
KST 2026-09-08 03:00  →  UTC 날짜 키 "2026-09-07"
```
자정부터 오전 9시까지는 여전히 "어제"로 취급되어, 일일 퀘스트가 오전 9시에 리셋됐습니다.

`challenge.js`에는 이미 로컬 기준 `localDateStr()`이 있었는데 app.js만 UTC를 쓰고
있었습니다. 같은 앱 안에서 "오늘"의 정의가 두 개였던 셈입니다. `localDateStr()`로 통일했습니다.
운동 기록의 timestamp 비교(`todayWorkouts`)와 챌린지 `startDate` 초기화도 함께 맞췄습니다.

> 배포 당일 한 번은 날짜 키가 바뀌어 일일 퀘스트를 다시 수령할 수 있습니다.
> 사용자에게 유리한 방향의 1회성 영향이라 별도 마이그레이션은 하지 않았습니다.

### 확인했으나 문제없던 것
- `localDateStr` / `daysBetween` — 월 경계, 역방향 모두 정상
- 하루에 여러 미션 몰아치기 — `isDayUnlocked` + `getNextMissionDay`로 차단됨
- 같은 운동 기록 재사용 — `dayRecords`의 `workoutId`로 차단됨
- 목표 미달·시간 0인 기록 거부

### 보고만 하고 고치지 않은 것
`findQualifyingWorkout()`은 운동 기록의 **날짜를 보지 않습니다.** 며칠 전 운동이
아직 쓰이지 않았다면 오늘 미션에 사용할 수 있습니다. 재사용은 막히지만
"쌓아둔 운동으로 나중에 미션 깨기"는 가능합니다.

이걸 막을지는 기획 판단입니다. 아침에 달리고 저녁에 앱을 여는 경우도 있어
무조건 오늘로 제한하면 정상 사용자가 손해를 볼 수 있습니다. 대표님 결정이 필요합니다.

## 2026-09-08 (2) — app.js 순수 계산 로직 분리

### 안전하게 옮기기 위해 지킨 것
1. **호출부를 한 줄도 바꾸지 않았습니다.** `calcBmi` / `bmiLabel` / `calcBmr`은
   `metrics.js`로 본체를 옮기고, app.js에는 같은 이름의 **위임 래퍼**만 남겼습니다.
   `this.calcBmi(...)`를 쓰던 6곳이 그대로 동작합니다.
2. **옮기기 전 동작을 테스트로 고정**한 뒤 옮겼습니다(기본값 대체, 문자열 입력,
   경계값 등 기존 구현의 quirk까지 포함).
3. **브라우저에서 원본 구현과 12건을 직접 대조**해 결과가 모두 같음을 확인했습니다.

### 별칭 임포트를 쓴 이유
위임 래퍼에서 모듈 함수와 메서드 이름이 같으면, 본문의 bare 식별자는 모듈 함수를
가리켜 정상 동작합니다(Node로 확인). 하지만 나중에 누군가 `this.`를 붙이는 순간
**무한 재귀**가 됩니다. 스택 오버플로는 원인 추적이 어려우므로
`calcBmi as calcBmiPure` 형태로 받아 실수 자체가 불가능하게 했습니다.

### 함께 정리한 것
러닝 완주 처리에 인라인으로 있던 페이스 파싱을 `parsePaceToSeconds()`로 뽑았습니다.
거리가 짧아 페이스를 못 구하면 GPSRunner가 `--'--"`를 주는데, 기존 코드는
`parseInt("--") || 6`으로 기본 6분을 만드는 방식이었습니다. 의도된 동작이지만
읽어서는 알기 어려워 함수로 만들고 주석과 테스트를 붙였습니다.

코인 계산도 `calcRunCoins()`로 분리하면서 **음수 거리 방어**를 추가했습니다.
기존 `Math.round(km * 20)`은 음수 거리가 들어오면 코인을 깎았습니다.

### 로컬 테스트 시 주의
`app.js?v=` 쿼리는 app.js만 갱신하고, app.js가 `import './challenge.js'`로 부르는
모듈은 브라우저 HTTP 캐시에 남습니다. 로컬 python 서버로 테스트할 때
"함수가 없다"는 에러가 나면 대개 이것입니다(강력 새로고침 필요).
Firebase Hosting은 js/css에 `no-store`를 주므로 배포 환경에서는 발생하지 않습니다.

### 남은 app.js
DOM·상태에 얽힌 부분(렌더링, 이벤트 바인딩, Firestore 동기화)은 그대로 두었습니다.
단위 테스트하려면 DOM 목이 필요한데, 비용 대비 실익이 크지 않다고 판단했습니다.

## 2026-09-09 — Cursor 작업은 내부 테스트 전용

대표님 지침. Cursor에서는 상용(`runnow.beauscreators.com`)에 배포하지 않는다.
일상 배포는 `npm run deploy:dev`만. 상용 승급은 대표님이 직접 말한 뒤에만 `npm run promote:live`.
QR/폰 링크 폴백을 테스트 채널 URL로 바꿨다. 페이지가 열린 origin이 있으면 그 주소를 쓴다.
규칙 파일. `.cursor/rules/runnow-internal-test-only.mdc` (alwaysApply)

## 2026-09-09 — 듀얼 트랙 전환 시점 전면 감사

### 감사 배경
내부테스트 채널(`runnow-37af9--dev-irl7g2ve.web.app`)이 개설되고 배포 헌장이
제정되면서, 앞으로 모든 작업이 "내부테스트 선행 → 대표님 수동 승급" 순서로 갑니다.
그 전제가 성립하는지 확인하고, 이후 작업을 정교하게 측정할 기준선을 만들었습니다.

### 측정 기준선 (2026-09-09)
- 테스트 292건 / 8개 스위트, 전부 통과
- JS 8,901줄 중 테스트가 있는 모듈 4,590줄(52%), 없는 모듈 4,311줄(48%)
- 미검증 모듈: app.js(3,318) catalog(252) firebaseClient(247) motionSound(201)
  paypalBridge(168) firebaseSandbox(125)
- 배포된 내부테스트 빌드에 그동안의 검증 수정이 전부 반영되어 있음을 원격으로 확인

### 발견 1 (P0) — 인프라가 물리적으로 격리되어 있지 않다
헌장 제1조 1항은 "인프라, 도메인, 브랜치 레벨에서 100% 물리적 격리"를 요구합니다.
실제로는 `.firebaserc`의 프로젝트가 `runnow-37af9` 하나뿐이라, 내부테스트 채널과
상용이 **같은 Firestore와 같은 Auth 사용자 풀을 공유**합니다.

격리된 것과 안 된 것을 나누면 이렇습니다.
- 격리됨: 호스팅 URL(오리진이 달라 localStorage·서비스워커 캐시도 분리), 브랜치
- 공유됨: Firestore 데이터베이스, Auth 사용자, (배포 시) Cloud Functions

즉 내부 테스터가 만든 러닝 기록·펫 데이터·구독 상태가 상용 DB에 그대로 쌓입니다.
상용 출시 후에는 실사용자 데이터와 테스트 데이터가 섞여 되돌리기 어렵습니다.

**지금이 분리 비용이 가장 싼 시점입니다.** 실사용자가 생기기 전이라 마이그레이션이
필요 없습니다. 별도 프로젝트(`runnow-dev`)를 만들고 `firebaseConfig.js`를
호스트명 기준으로 분기하면 됩니다. 다만 프로젝트 생성은 대표님 계정 작업입니다.

### 발견 2 (P0) — 결제 서버 검증이 배포되지 않았다
브라우저에서 함수 4개(createPaypalOrder / capturePaypalOrder / getMySubscription /
paypalWebhook) 모두 접근 불가였고, `firebase functions:list`도 실패했습니다.
Blaze 전환이 아직 안 된 것으로 보입니다.

역설적으로 헌장 제2조 3항("테스트 환경에서 실제 청구 0원")은 **충족된 상태**입니다.
`paypalBridge.js`가 `setTimeout` 후 가짜 주문번호를 만드는 시뮬레이션이라
실제 청구가 일어날 수 없기 때문입니다. 다만 이는 안전장치가 작동해서가 아니라
결제 기능 자체가 없어서입니다. 상용 승급 시점에는 결제가 아예 동작하지 않습니다.

### 발견 3 (P1) — 매 페이지 로드마다 404가 나는 죽은 스크립트
`index.html:80`이 `@mediapipe/camera_utils@0.4/camera_utils.js`를 로드하는데,
jsdelivr에서 해당 경로가 **404**입니다(curl로 확인). 404 응답이 `text/plain`이라
브라우저가 MIME 오류까지 냅니다.

그리고 `camera_utils`는 코드 어디에서도 쓰이지 않습니다. `motionTracker.js`는
`window.Pose`(pose.js)만 사용하고 카메라는 `getUserMedia`로 직접 엽니다.
같은 줄에 있는 `pose.js`는 200 정상 응답입니다.

기능 영향은 없지만, **콘솔에 상시 에러가 떠 있으면 진짜 오류를 놓칩니다.**
실기기 테스트를 앞둔 지금 제거하는 것이 맞습니다.

### 판정: 광고 격리는 헌장보다 강하게 구현되어 있다
헌장은 `data-adtest="on"` 테스트 모드를 요구하는데, 실제 구현은 테스트 환경에서
**AdSense 스크립트를 아예 로드하지 않습니다**(`index.html:56` 인라인 가드).
스크립트가 없으면 광고도 클릭도 없으므로 제재 리스크가 구조적으로 0입니다.
배포된 dev 채널에서 `광고스크립트_로드됨: false`를 직접 확인했습니다.

`app.js`의 `data-adtest="on"` 주입 로직은 상용 도메인에 접속한 테스터를 위한
2차 방어선으로 남아 있어, 이중 방어가 됩니다.

### 이번 감사에서 코드를 고치지 않은 이유
헌장 제2조에 따라 모든 변경은 내부테스트 배포와 검증을 거쳐야 합니다.
P0 두 건은 대표님 계정 작업(프로젝트 생성, Blaze 전환)이 선행되어야 하고,
P1은 즉시 고칠 수 있으나 승인 후 `deploy:dev`로 반영하는 것이 순서에 맞습니다.

## 2026-09-09 (2) — 죽은 스크립트 제거 및 엔진 우선순위 전환

### 작업 중 발견한 것
P1(죽은 스크립트 제거)을 하려고 보니 `index.html`에 **커밋되지 않은 변경**이 이미 있었고,
`camera_utils.js`(죽은 것)와 함께 **`pose.js`(실제 사용 중)까지 제거**되어 있었습니다.
두 줄이 같은 주석 아래 붙어 있어 함께 지워진 것으로 보입니다.

그 상태의 영향은 이랬습니다.
- `waitForClassicPose()`가 1.5초를 기다렸다 실패 → 카메라 시작이 매번 1.5초 지연
- Classic Pose 경로 사망 → 사실상 Tasks Vision 전용

### 결정: Tasks Vision 1순위 + Classic 폴백
원상복구(Classic 1순위)와 현상유지(Tasks Vision 전용) 사이에서 대표님이 세 번째 안을
선택하셨습니다.

근거는 이전 감사에서 이미 나와 있었습니다. **Classic Pose는 1명만 반환**하므로
`selectSubjectPose()`가 후보 중에서 고를 수가 없습니다. 즉 "여러 사람이 있으면 남의 동작이
카운트된다"는 원래 문제의 근본 방어선이 Classic 경로에서는 작동하지 않습니다.
Tasks Vision은 `numPoses: 3`으로 후보를 받아 신체 서명이 맞는 사람을 고를 수 있습니다.

동시에 Classic을 완전히 버리지는 않았습니다. Tasks Vision은 WASM과 5.7MB 모델 다운로드가
필요해 구형 기기에서 실패할 수 있고, 그때 AI 운동을 아예 못 쓰게 되기 때문입니다.

### 구현
`initMediaPipe()`가 두 엔진 로직을 통째로 품고 있어 순서만 바꾸기 어려웠습니다.
`tryTasksVision()` / `tryClassicPose()`로 분리하고 `initMediaPipe()`는 호출 순서만
결정하도록 정리했습니다. 각 엔진의 내부 로직은 그대로 옮겼습니다.

### 검증
- 자동 테스트 292건 유지
- 로컬에서 두 메서드를 스텁으로 바꿔 **호출 순서가 tasks_vision → classic_pose**임을 확인
- 내부테스트 배포 후 원격 확인
  - 이번 로드 오류 **0건** (이전에는 404 + MIME 오류 상시 발생)
  - 로드된 외부 스크립트가 `pose.js` 계열뿐
  - AdSense 스크립트 여전히 차단됨

`camera_utils` 문자열이 HTML에 남아 있는데, 이는 제가 넣은 **주석**입니다.
script 태그로는 존재하지 않음을 별도로 확인했습니다.

### 아직 검증 못 한 것
실기기에서 어느 엔진이 실제로 선택되는지는 확인하지 못했습니다.
HUD 좌하단 엔진 태그(`Tasks Vision · 다중 인원` / `Pose Classic · 1인`)로 확인 가능합니다.
구형 기기에서 Classic으로 폴백되면 다중 인원 선별이 동작하지 않으므로,
그 경우 사용자에게 안내할지 판단이 필요합니다.

## 2026-09-09 (3) — 운동자 잠금 설계 결함 발견 및 수정

### 대표님 지적에서 출발
"혼자 하면 숫자가 어긋날 수 있는 거 아니냐"는 질문을 받고 실측했습니다.
추측으로 답하지 않고 서명 거리를 직접 측정한 것이 결정적이었습니다.

### 측정 결과 — 설계가 뒤집혀 있었다
임계값 1.6 기준으로 잰 값입니다.

| 상황 | 최대 거리 | 판정 |
|---|---|---|
| 제자리 스쿼트 5회 | 1.372 | 인정 (여유 14%) |
| **흔들리며 스쿼트 5회** | **1.778** | **거부** |
| [비교] 비슷한 체형이 바로 옆 | 0.893 | 인정 |

**내 스쿼트 동작이 옆 사람보다 더 "남처럼" 보입니다.** 임계값을 올리면 남을 물고,
내리면 나를 놓칩니다. 잠금 추적 속도(alpha)를 0.3~1.0으로 바꿔가며 재봤지만
모든 구간에서 역전이 유지돼, **절대 임계값으로는 분리 불가능**한 구조였습니다.

### 수정 1 — 절대 거부 폐지
`SUBJECT_MATCH_TOLERANCE`를 제거했습니다. 대신 이렇게 바꿨습니다.
- 검출 1명 → 무조건 인정. 고를 것이 없는데 거부하면 **혼자 운동하는 대다수가 손해**입니다.
- 검출 2명 이상 → 서명이 가장 가까운 후보를 고르되 절대 거부는 하지 않습니다.

잃는 것은 "본인이 나가고 낯선 사람이 혼자 남으면 그 사람이 카운트되는" 경우인데,
그때는 본인이 자리에 없으므로 실질 피해가 없습니다. 반면 혼자 운동 중 거부는
매 세션 발생하는 실질 손해입니다.

### 수정 2 — 세로 이동 가중치 하향 (VERTICAL_WEIGHT = 0.4)
1명일 때를 해결해도 2명일 때 45프레임 중 5건에서 옆 사람을 골랐습니다.

원인을 다시 보니 방향성이 있었습니다. **운동 중에는 몸 중심이 위아래로 크게 움직이고
(스쿼트 한 번에 화면의 약 19%), 사람들은 좌우로 떨어져 섭니다.** 세로를 가로와
동등하게 보면 내 상하 운동이 옆 사람과의 좌우 거리를 압도합니다.

세로 가중치를 낮춰가며 측정한 결과입니다.

| 세로 가중치 | 혼자 거부 | 둘일 때 오선택(45중) |
|---|---|---|
| 1.0 (기존) | 0 | 5 |
| 0.6 | 0 | 0 |
| **0.4 (채택)** | **0** | **0** |
| 0.05 | 0 | 0 |

0.6에서 이미 0이 되지만 여유를 두고 0.4를 택했습니다. 더 낮추면 세로 정보를
거의 버리게 되어, 앞뒤로 겹쳐 선 사람 구분이 약해질 수 있습니다.

### 회귀 테스트 2건 추가
- 혼자 스쿼트 5회 45프레임 → 거부 0건
- 옆 사람도 함께 스쿼트 45프레임 → 오선택 0건

이 두 개가 깨지면 실사용에서 "혼자인데 카운트를 놓친다" 또는 "옆 사람이 카운트된다"가
재발한다는 뜻입니다. 총 294건.

### 남은 한계 (정직하게)
합성 좌표 기준입니다. 실제 카메라 앞 두 사람으로는 아직 검증하지 못했습니다.
또한 4명 이상이면 `numPoses: 3` 제한으로 본인이 검출 대상에서 빠질 수 있습니다.

## 2026-09-10 — 프로젝트 폴더 `proj_01` → `Runnow`

폴더 실경로. `c:\BeausCreators\03.Research\바이브코딩 연구\Test_proj\Runnow`
npm package name은 `runnow`. 문서 빌드 스크립트는 `__file__` 기준이라 이후 폴더 이동에도 따라간다.
Firebase 사이트 ID(`runnow-37af9`)와 테스트 URL은 폴더명과 무관하므로 그대로 둔다.

## 2026-09-10 — 실기기 러닝 테스트 피드백 대응 (GPS 정확도)

### 보고받은 증상
어제 야외 러닝과 헬스장에서 테스트한 결과입니다.
1. 가만히 서 있는데 거리가 2m 이상 늘어남
2. 약 2초 딜레이
3. AVG PACE가 `--'--"`로 표시되며 특히 심각함

### 원인 1 — 노이즈 임계값이 GPS 오차보다 작았다 (제가 만든 문제)
`MIN_STEP_METERS = 1.5` 고정값을 썼습니다. 그런데 야외 GPS 오차는 보통 ±5~15m입니다.
**오차 반경 안에서 좌표가 흔들리는 것만으로 1.5m를 우습게 넘습니다.** 서 있어도 거리가
쌓이는 게 당연했습니다.

수정: 임계값을 **기기가 보고한 정확도에 비례**하게 만들었습니다(`accuracy × 0.7`, 하한 1.5m).
±12m 환경이면 8.4m를 넘어야 이동으로 인정합니다.

핵심은 임계값에 못 미친 이동이 **버려지지 않는다**는 점입니다. 기준점이 유지된 채
누적되므로, 실제 러닝은 방향성이 있어 금방 임계값을 넘고 거리가 보존됩니다.
반면 제자리 흔들림은 한 점 주위를 맴돌아 누적되지 않습니다.

같은 이유로 `MAX_ACCURACY_M`을 25 → 40으로 완화했습니다. 임계값이 정확도에 비례하니
오차가 큰 측정치는 스스로 통과하기 어려워져, 문턱에서 통째로 버릴 필요가 줄었습니다.

### 원인 2 — AVG PACE 분모에 GPS 대기 시간이 들어갔다
페이스를 `elapsedSeconds / distanceKm`로 계산했는데, `elapsedSeconds`는 **START 버튼을
누른 순간부터** 셉니다. GPS 신호를 잡는 데 걸린 시간, 준비하며 서 있던 시간이 전부
분모에 포함됩니다.

실측 시나리오로 확인했습니다. GPS 대기 90초 + 실제 1km를 300초에 주행하면
- 기존: 390초/km → **6'30"** (실제보다 30% 느림)
- 대기 시간이 더 길면 60분/km를 넘어 `--'--"`로 표시됨

수정: 첫 유효 이동이 관측된 시점부터를 "달린 시간"으로 삼습니다.
이때 기준을 "지금"이 아니라 **그 이동이 걸쳐 있던 구간의 시작점**(`elapsedSeconds - dt`)으로
잡아야 첫 구간의 소요 시간이 빠지지 않습니다. 이 한 줄 차이로 4'54"와 5'00"이 갈렸습니다.

총 경과 시간(`elapsedSeconds`)은 그대로 두고 표시하므로, 러닝 시간 기록은 영향받지 않습니다.

### 원인 3 — 2초 딜레이
일부는 원인 1의 부작용입니다. 1.5m가 쌓일 때까지 숫자가 안 움직여 뚝뚝 끊겨 보입니다.
나머지는 스마트폰 GPS 자체의 물리적 지연으로, 코드로 없앨 수 없습니다.
`watchPosition`은 이미 `enableHighAccuracy: true`, `maximumAge: 0`으로 최선의 설정입니다.

### 추측을 줄이기 위한 진단 표시 추가
어제처럼 "수치가 이상하다"는 보고를 받았을 때 코드만 보고 원인을 좁히는 데 한계가
있었습니다. 내부테스트 환경에서만 GPS 배지 아래에 실시간 진단값을 노출합니다.

```
🛰️ GPS 매우양호 (±8m)
오차 ±8m · 인정구간 42 · 정확도폐기 3 · 달린시간 310s
```

- `인정구간`이 안 늘면 → 임계값이 너무 높음
- `정확도폐기`가 계속 늘면 → 신호 환경 문제
- `달린시간`과 총 시간 차이 → GPS 대기 시간

상용에서는 표시되지 않습니다(`__IS_INTERNAL_TEST_ENV__` 기준).

### 회귀 테스트 5건 추가 (총 299건)
- ±12m 정확도로 60초 정지 → 누적 거리 0
- ±3m 정확도에서 5m 이동은 인정 (과도한 필터링 방지)
- GPS 대기 90초 후 1km/300초 → 페이스 정확히 5'00"
- 대기 시간 제외 후에도 총 경과 시간은 보존

### 아직 확인 못 한 것
헬스장(트레드밀) 모드는 이번 수정 범위 밖입니다. `tickTreadmill`이 DeviceMotion으로
걸음을 감지해 속도를 누적하는데, 실기기 센서가 필요해 검증하지 못했습니다.
헬스장에서도 수치가 이상하셨다면 별도로 봐야 합니다.

## 2026-09-10 (2) — 네이티브 운동앱 수준을 목표로 한 GPS 전면 개선

### 요청
"삼성·애플 운동앱처럼 정확했으면 좋겠다."

### 먼저 정리한 현실
웹앱은 OS 센서 융합(가속도·자이로·기압계)과 백그라운드 실행에 접근할 수 없습니다.
따라서 "완전히 동일"은 불가능하며, 목표를 **"화면을 켜고 달리는 동안은 실용적으로 충분히
정확"** 으로 잡고 5가지를 모두 구현했습니다.

### ① Wake Lock — 러닝 화면에 아예 빠져 있었다
`motionTracker`(AI 운동)에는 있는데 `gpsRunner`(러닝)에는 없었습니다.
화면이 꺼지면 브라우저가 타이머와 GPS를 정지시켜 그 구간이 통째로 사라집니다.
어제 "딜레이가 있고 수치가 이상하다"는 보고의 상당 부분이 이것이었을 가능성이 큽니다.

### ④ 화면 복귀 감지
`visibilitychange`로 꺼졌다 켜진 시점을 인지하고, 복귀 직후 첫 구간은 거리에 넣지 않습니다.
이때 **필터 이력도 함께 버립니다**. 안 버리면 필터가 옛 위치를 향해 따라오는 만큼이
다음 이동 거리에 얹힙니다(테스트에서 10m 걸었는데 29m로 나와 발견).

### ② 칼만 필터 — 문턱 방식을 대체
기존은 "N미터 넘으면 인정"하는 문턱 방식이었습니다. 노이즈를 못 걸러내거나,
걸러내려고 문턱을 올리면 숫자가 뚝뚝 끊겨 딜레이처럼 보입니다.
오차 가중 위치 추정기로 바꿔 매 측정을 부드럽게 반영합니다.

### ③ OS 제공 speed 활용 — 정지 판정의 핵심
구현 도중 시뮬레이션으로 재보니 **주행은 좋아졌는데 정지에서 여전히 71~120m가 쌓였습니다.**
칼만 필터만으로는 부족했습니다. 필터가 "초속 3m로 움직일 수 있다"고 가정해 노이즈를
따라갔기 때문입니다.

해법은 `coords.speed`였습니다. 이 값은 도플러 기반이라 좌표 차이보다 훨씬 정확하고,
**0에 가까우면 실제로 멈춰 있다**는 뜻입니다. 이걸 정지 판정에 쓰자 0m가 됐습니다.
네이티브 앱이 정지를 아는 방식도 이것입니다.

속도를 제공하지 않는 기기(구형·실내 wifi)는 별도 처리했습니다. 처음엔 변위 기반
판정을 시도했으나 실패했습니다. **±25m 노이즈에서 무작위 변위는 초속 1m 걷기와
통계적으로 구분이 불가능**하기 때문입니다(물리적 한계). 대신 오차 반경만큼 확실히
움직여야 인정하는 보수적 기준(`ACCURACY_STEP_RATIO_NO_SPEED = 1.1`)으로 해결했습니다.

### ⑤ 최근 30초 기준 현재 페이스
전체 평균은 초반 대기와 중간 휴식에 끌려다녀 "지금 어느 정도로 달리는지"를 못 보여줍니다.
AVG PACE 아래에 "지금 N'NN"" 을 함께 표시합니다.

### 측정 결과 (재현 가능한 난수로 실제 GPS 노이즈 시뮬레이션)

| 시나리오 | 속도 제공 기기 | 속도 미제공 기기 |
|---|---|---|
| 정지 60초 ±12m | **0m** | **0m** |
| 정지 300초 ±25m | **0m** | **0m** |
| 1km 주행 ±8m | +1.1% | −1.1% |
| 5km 주행 ±10m | +1.7% | +0.2% |
| 느린 걷기 500m | −1.7% | −0.2% |

개선 전에는 같은 조건에서 정지 60초에 71m, 정지 300초에 120m가 쌓였습니다.

### 부수적으로 고친 것
`Date.now()` 대신 `position.timestamp`(GPS 측정 시각)를 쓰도록 바꿨습니다.
브라우저 처리 지연이 섞이면 속도 계산과 필터의 시간 간격이 왜곡됩니다.

### 테스트 조정에 대한 기록
칼만 필터 도입으로 기존 GPS 테스트 여러 개가 실패했습니다. 확인해 보니
**단발 점프(1회에 111m 이동)로 먹이는 방식**이 문제였습니다. 실제 GPS는 초당 1회씩
주므로 필터가 수렴할 기회가 있는데, 단발로 주면 없습니다.
- 하버사인 공식 자체는 순수 함수로 직접 검증하도록 분리(`calculateDistanceMeters` export)
- 누적 동작은 초당 1샘플의 현실적인 시퀀스로 변경

테스트가 실패했을 때 임계값을 완화하는 대신 **왜 실패하는지 먼저 확인**한 것이
"단발 점프는 비현실적"이라는 진짜 원인을 찾게 했습니다.

### 남은 한계 (정직하게)
- 백그라운드 실행은 여전히 불가능합니다. Wake Lock으로 화면을 켜둘 뿐입니다.
- 트레드밀 모드(`tickTreadmill`)는 이번 범위 밖입니다. DeviceMotion 센서가 필요해
  실기기 없이는 검증할 수 없습니다. 헬스장 수치가 계속 이상하면 별도로 봐야 합니다.
- 시뮬레이션은 백색 노이즈 모델입니다. 실제 GPS 오차는 시간적으로 상관되어 있어
  실기기 결과는 다를 수 있습니다. 반드시 야외 검증이 필요합니다.

## 2026-09-10 (3) — 운동 수치 계산을 공개 표준으로 전환

### 조사한 것
나이키·삼성·애플이 쓰는 계산은 비공개 알고리즘이지만, 그 기반이 되는 표준은 공개되어 있습니다.
- **ACSM 대사 방정식**: 속도와 경사로부터 산소 소비량(VO2)을 추정
- **Compendium of Physical Activities**: 운동별 MET 값 (1 MET = 3.5 ml/kg/min)
- 산소 1리터 ≈ 5 kcal (운동생리학 표준 환산)

### 기존 공식과의 차이를 먼저 측정했다
`거리 × 체중 × 1.036` 공식을 ACSM과 비교한 결과입니다(70kg, 5km).

| 상황 | 기존 | ACSM | 오차 |
|---|---|---|---|
| 달리기 10km/h | 363 | 350 | +4% |
| 달리기 12km/h | 363 | 350 | +4% |
| **빠른 걷기 5km/h** | **363** | **175** | **+107%** |
| 달리기 + 오르막 5% | 363 | 429 | −15% |
| 달리기 + 오르막 10% | 363 | 508 | −29% |

**평지 달리기는 기존 공식이 이미 정확했습니다**(+4%). 문제는 두 가지였습니다.
1. 걷기와 달리기를 구분하지 못해 걷기를 **2배 과대계산**
2. 경사를 반영하지 못해 오르막을 최대 29% 과소계산

기존 계수 1.036이 우연히 "달리기 순수 에너지 비용(약 1 kcal/kg/km)"과 비슷했던 것이며,
이는 달리기에서만 성립합니다.

### 채택한 기준: 활동 칼로리(net)
안정 시 대사량(1 MET)을 뺀 값입니다. 애플이 "Active Energy"로 표시하는 것과 같은 기준입니다.
가만히 있어도 소비되는 양까지 운동 성과로 계산하면 과대평가가 됩니다.

### 근력 운동: 회당 고정값 → MET × 시간
기존은 `caloriePerRep × 횟수`였습니다. 같은 20회를 5분에 하든 10분에 하든 같은 값이
나와 실제 소비와 어긋납니다. Compendium MET로 바꿨습니다.

| 종목 | MET | 근거 |
|---|---|---|
| 푸시업·싯업·점핑잭 | 8.0 | 칼리스테닉스 고강도 |
| 스쿼트 | 5.0 | 전용 항목 |
| 런지 | 4.0 | 칼리스테닉스 중강도 |
| 플랭크 | 3.8 | 등척성 중강도 |

`caloriePerRep` 필드는 남겨두었습니다(다른 곳에서 참조할 수 있어 제거는 별도 판단).

### 경사는 의도적으로 아직 반영하지 않았다
ACSM 방정식은 경사를 지원하고 모듈에도 구현·검증했지만, **칼로리 계산에는 연결하지
않았습니다.** GPS 고도는 수평 좌표보다 오차가 2~3배 크고, 잘못된 경사는 칼로리를
크게 왜곡합니다(10% 경사 오판 시 +45%).

대신 고도를 수집해 **진단 표시에만 노출**합니다. 실기기에서 고도 값이 쓸 만한지 본 뒤
반영 여부를 정하는 것이, 앞서 정확도 필터를 추측으로 넣었다가 실기기에서 문제를
일으킨 경험에 비춘 판단입니다.

### 검증
`metabolics.js` 테스트 30건을 교과서 예제값과 대조했습니다.
- 달리기 10km/h VO2 = 36.83 (0.2×166.67 + 3.5)
- 걷기 5km/h VO2 = 11.83 (0.1×83.33 + 3.5)
- 70kg 30분 → 350 kcal
- 걷기 5km가 달리기 5km의 절반 수준으로 나오는지 확인

총 348건.

### 조사 출처
- Compendium of Physical Activities (pacompendium.com)
- ACSM 대사 방정식 (달리기·걷기 VO2 추정 공식)

## 2026-09-10 (4) — 헬스장 거리는 기계 화면이 정본

속도 칩(6/8/10/12)과 폰 흔들림으로 거리를 만들던 방식을 뺐다.
달리는 중에는 타이머만 돌리고, 끝나면 트레드밀 콘솔 km를 입력한다.
시속 30km를 넘는 입력은 거부한다. 펫·퀘스트·칼로리는 그 숫자로 정산한다.
폰을 콘솔에 둬도 된다. DeviceMotion 게이트를 쓰지 않는다.
