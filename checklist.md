# 분석 후 조치 체크리스트 (2026-09-15)

397건 테스트 통과 상태에서 발견한 항목을 순서대로 처리합니다.

## Phase 0. 기존 작업 보존
- [x] 0-1. 미커밋 38개 파일 커밋 (롤백 지점 확보)
      → 검증: 테스트 397건 유지, 작업 트리 정리

## Phase 1. 케어팀 페이월 (매출 직결, 최우선)
- [x] 1-1. `tab-careteam`을 구독 게이팅 목록에 추가
      → 근거: PRO·VIP 두 요금제 모두 케어팀 접근을 상품 설명에 포함
- [x] 1-2. 회귀 테스트 추가
      → 검증: 무료 사용자는 진입 불가, 구독자·테스터는 통과

## Phase 2. dev 환경 판정 범위 축소
- [x] 2-1. `host.includes('dev')` → `--dev-` 기준으로 좁힘
      → 이유: 도메인에 'dev'가 들어가면 상용이 dev로 오인되어 상용 데이터가 dev_에 기록됨
- [x] 2-2. 판정 테스트 추가
      → 검증: 상용 도메인은 prod, dev 채널·localhost는 dev

## Phase 3. 운동 이미지 최적화
- [x] 3-1. 표시 크기 확인 후 목표 해상도 결정
- [x] 3-2. 리사이즈 (원본은 보존)
- [x] 3-3. lazy 로딩 적용
      → 검증: 합계 5MB → 대폭 감소, 화면상 화질 유지

## Phase 4. 마무리
- [x] 4-1. 전체 테스트 통과
- [x] 4-2. 내부테스트 채널 배포 및 확인

## 대표님 결정 필요 (AI 단독 진행 불가)
- [ ] VIP($19.99) 차별 기능 — 서버 제공 AI를 만들 것인가, 상품 설명을 바꿀 것인가

## Phase 5. 표시광고 정합성
- [x] 5-1. VIP 카드 ₩49,000 취소선 제거 (index.html)
- [x] 5-2. 요금제 정의의 "(정가 ₩49,000)" 제거 (subscriptionManager.js)
- [x] 5-3. 종전가격 회귀 테스트 추가
      → 검증: 취소선 복원 시 FAIL, 제거 상태에서 PASS (460개 전체 통과)

## Phase 6. Gemini 모델 정합성
- [x] 6-1. 종료된 모델(gemini-1.5-flash) 호출 제거 — PRO AI가 404로 죽어 있었음
- [x] 6-2. BYOK 실패를 사용자에게 노출 (조용한 템플릿 폴백 제거)
- [x] 6-3. 서버 기본 모델을 별칭 → 버전 고정 (단가 가정 2.3배 어긋남 해소)
- [x] 6-4. 공시 단가를 코드에 명시하고 테스트가 마진을 재계산하도록 연결
- [x] 6-5. 회귀 테스트 추가 (tests/geminiModel.test.mjs)
      → 검증: 사고 상황 재현 시 FAIL, 정상 상태 PASS (472개 전체 통과)
- [ ] 6-6. (운영 시작 후) chatWithCoach 실패 로그 알림 연결 — 대표님 결정 필요

## Phase 7. Gemma 4 실제 대화 + 모던 미니멀 기본 테마 (2026-09-16)
- [x] 7-1. `gemmaOnDevice.js` LiteRT-LM Gemma 4 E2B 엔진
- [x] 7-2. 케어팀 라우팅 VIP Gemini → Gemma → BYOK → 간단 모드
- [x] 7-3. 최근 대화 이력·되묻기 프롬프트. 대사집은 배지 표시
- [x] 7-4. 탭 진입 자동 다운로드 없음. Wi-Fi 2GB 동의 후 받기
- [x] 7-5. 요금제 문구에 Gemma 선판매하지 않음
- [x] 7-6. 기본 테마를 모던 미니멀(`minimal`)로 변경
- [x] 7-7. 테스트 통과 후 내부테스트 채널 배포·브라우저 확인

## Phase 8. 자세 게이트 · 날씨 정합성 · 회귀 복구 (2026-09-16)

### 8-1. 모션 오판 (영상 증거: 앉아서 손 흔들기 → 푸시업 9회)
- [x] 푸시업·플랭크 엎드림 게이트 (몸통 수평 45도 이내 + 어깨·골반 최소 간격)
- [x] 스쿼트·런지 기립 게이트 (몸통 비수평 + 정강이 세로 = 책상 다리뻗기 차단)
- [x] 윗몸일으키기 시작 자세 게이트 (몸통 수평 30도 이내 = 의자 흔들기 차단)
      → 45도로는 골반각 140도 등받이 자세가 통과함. 30도로 좁힘
- [x] 점핑잭 기립 게이트 (누워서 팔다리 휘젓기 차단. 점프 때문에 정강이 검사는 제외)
- [x] 자동 보정 기본 ON + 정밀 모드 토글로 전환
- [x] UI 모순 제거 (카운트 전 "PERFECT FORM!" 배너 노출 중단)
- [x] 런지 뒷무릎 게이트 — 판정값이 좌우 최소값이라 서서 한쪽만 굽혀도 세어졌음
- [x] 점핑잭 측면 촬영 거부 안내 — 어깨폭이 0.02로 무너져 영구 오누락이던 경로
- [x] 점핑잭 코 신뢰도 가드 — 머리가 잘리면 가슴 높이 손도 "머리 위"로 읽혔음
- [x] 점핑잭 필수 랜드마크에 골반 추가 (몸통 방향·운동자 잠금이 골반을 읽음)
- [x] 9종 회귀 테스트 추가 — 오탐 자세는 0회, 정상 스쿼트·런지는 그대로 인정
- [ ] (보류) 푸시업 `formOk` 페일오픈 + 필수 랜드마크 발목 누락
      → 카운트가 아니라 허리 처짐 피드백만 영향. 단련자 난이도 한정.
      → 발목을 필수로 올리면 상반신만 프레이밍한 사용자가 시작조차 못 하는 쪽이 더 큰 손해
- [ ] (보류) 스쿼트·윗몸·점핑잭의 `formOk: true` 상수 → 초·중급은 `strictForm`이 꺼져 있어
      폼 게이트가 무의미. 자세 게이트를 `formOk`가 아니라 측정 거부로 넣은 이유

### 8-2. 날씨 불일치 (증거: 앱 기온 ≠ 폰 날씨 앱)
- [x] 캐시 키에 좌표 추가 — 직전에 본 수원 날씨가 15분간 현재 위치로 표시되던 문제
- [x] 무키 역지오코딩 도입 (동 단위 지명). 좌표 구간 추정은 폴백으로만
- [x] 좌표 구간 경계 수정 (서울 서쪽 126.8 → 126.75. 강서·부천이 탈락했음)
- [x] 타임존 `Asia/Tokyo` → `Asia/Seoul`
- [x] 강수확률을 `hourly[0]`(자정) → 현재 시각 값
- [x] API 실패 시 가짜 "20.5℃ · 95점 골든 아워" 제거. 빈 값과 연결 실패 안내로 대체
- [x] 드롭다운에 "현재 위치" 항목 + 실제 조회 위치와 동기화
- [x] 격자 보간값에 붙던 기상청 관측소 명칭 제거 (없는 권위)
- [x] 회귀 테스트 추가 (tests/weather.test.mjs)

### 8-3. 되돌아간 수정 복구 (Phase 5·6이 다시 깨져 있었음)
- [x] `careTeam.js`가 종료된 `gemini-1.5-flash`를 다시 호출하고 있었음 → 서버와 같은 모델로 고정
- [x] BYOK 실패 안내(`byokFailureNotice`) 재구현 + 상태 코드 보존
- [x] VIP 카드 ₩49,000 취소선이 다시 붙어 있었음 → 제거
      → 교훈: 이미 체크한 항목도 테스트가 없으면 다음 수정에서 조용히 되돌아감

### 8-4. 다마고치 정합성 (실패 43건 → 111건 통과)
- [x] 낡은 테스트 37건 정정 (5→10단계 개편, 운동·컨디션 라벨 변경)
- [x] 실제 버그 6건 수정
      - 시간 감쇠 하한선이 회복으로 작동 → 방치만으로 체력 20까지 자동 회복되어
        "기력 부족 시 놀아주기 실패" 가드가 무력화(실패해야 할 액션이 XP 25 지급)
      - 스탯 상한 100 미적용 → 30km 한 번에 근력 +245, stat_all_100 퀘스트가 한 번에 뚫림
      - `addKmAndWorkout()`이 `earnedXp` 미반환 → 완주 모달이 항상 "+0 XP"
      - `rescue()`가 대사 기준 시각 미갱신 → 코인으로 산 풀충전이 즉시 깎임
- [x] 상한 우회 호출부 차단 (app.js·workout.html이 `pet.might = ...`로 직접 대입했음)
      → `applyStatGrowth()` 경유 + 실제 증가분을 로그에 되돌려 기록
      → 직접 대입이 되살아나면 FAIL하는 테스트 추가
- [ ] (대표님 확인) 3단 컨디션 라벨(EXHAUSTED/DEPRESSED/BEST_CONDITION)이 기획이라면 코드 구현 필요
      → 현재 코드·화면에 존재한 적 없는 라벨이라 테스트를 현행 2단(TIRED/HEALTHY) 기준으로 맞춤

### 8-5. 마무리
- [x] 전체 18개 스위트 통과 · 검증 625건
- [x] 내부테스트 채널 배포 — https://runnow-37af9--dev-irl7g2ve.web.app (2026-10-16 만료)
