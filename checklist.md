# GPS 정확도 개선 체크리스트 (2026-09-10)

목표: 네이티브 운동앱(삼성 헬스·애플 피트니스)에 실용적으로 근접.
전제: 웹앱은 OS 센서 융합과 백그라운드 실행에 접근할 수 없으므로 완전 동일은 불가능.
현실적 목표는 "화면을 켜고 달리는 동안은 충분히 정확".

## ① 화면 꺼짐 방지 (Wake Lock)
- [x] 1-1. `gpsRunner`에 Wake Lock 도입 (motionTracker에는 있으나 러닝에는 누락)
- [x] 1-2. 러닝 종료·일시정지 시 해제 → 검증: 러닝 중 화면이 꺼지지 않음

## ④ 백그라운드 복귀 감지
- [x] 4-1. `visibilitychange`로 화면이 꺼졌다 켜진 구간 인지
- [x] 4-2. 복귀 직후 첫 좌표는 기준점만 옮기고 거리에 넣지 않음
     → 검증: 정지 구간 이후 튄 좌표가 거리로 잡히지 않음

## ② 칼만 필터 (핵심)
- [x] 2-1. 오차 가중 위치 추정기 도입 (문턱 방식 대체)
- [x] 2-2. 필터링된 좌표 간 거리로 누적 계산
- [x] 2-3. 잔여 노이즈용 최소 게이트만 유지
     → 검증: ±12m 노이즈로 정지 60초 → 거리 0
     → 검증: 노이즈 있는 직선 1km 주행 → 오차 5% 이내

## ③ OS 제공 speed 활용
- [x] 3-1. `coords.speed`를 프로세스 노이즈 추정에 반영 (적응형)
- [x] 3-2. 안티치트 속도 판정에 OS 값 우선 사용
     → 검증: 기존 안티치트 테스트 유지

## ⑤ 최근 30초 기준 현재 페이스
- [x] 5-1. 롤링 윈도우 페이스 계산
- [x] 5-2. HUD에 표시 (AVG PACE와 별도)
     → 검증: 속도 변화 시 즉시 반영

## 마무리
- [ ] 전체 테스트 통과 유지
- [ ] 내부테스트 채널 배포 및 진단 표시 확인
